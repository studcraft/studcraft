# Generated ruleset index (built by scripts/build_index.py; docs/ is the source of truth)
.studcraft/

# Python bytecode from the scripts in scripts/
__pycache__/

# The local test environment. scripts/ stays stdlib-only; pytest is a
# development dependency and lives here, per requirements-dev.txt.
.venv/
.pytest_cache/

# Local scratch handovers, deliberately never committed
delete-me*
